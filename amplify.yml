version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
        - npm run build
        - npm install --prefix cdk
        - npm run --prefix cdk deploy
  artifacts:
    baseDirectory: public
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - cdk/node_modules/**/*
